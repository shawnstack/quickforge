---
description: Draft, edit, review, or structure a document deliverable.
argument-hint: "[document request]"
allow_edit: true
allow_commands: false
---

Use the Documents skill for this request. Produce a high-quality document deliverable that matches the user's goal, audience, tone, language, length, and requested format.

Request:

$ARGUMENTS

If the user asked to create or edit files, first determine the appropriate workspace path. If the path is ambiguous, ask a concise clarification before writing. Prefer Markdown unless the user requested a different format.
