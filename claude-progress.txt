Progress Log - FastCode
Updated: 2026-02-14

Completed this session
- Read requirement sources: MIGRATION_PLAN.md, run-codex-init.sh, run-codex-loop.sh.
- Established Rust baseline project files: Cargo.toml, src/main.rs, src/lib.rs, .gitignore.
- Created required handoff artifacts:
  - app_spec.txt
  - feature_list.json
  - init.sh and init.ps1
  - README.md
  - claude-progress.txt
- Created baseline folders: src/, tests/, docs/, scripts/.
- Verified bootstrap commands locally via:
  - powershell -ExecutionPolicy Bypass -File ./init.ps1
  - sh ./init.sh
  - cargo run -- --mode edit

Risks / Issues
- MIGRATION_PLAN.md contains encoding corruption in parts of Chinese prose; interpreted strictly via intact structure/snippets.
- Core modules from migration plan are not implemented yet (currently bootstrap-level executable only).

Verified checklist items
- F-001 passed with script execution evidence.
- F-002 passed with CLI runtime evidence.

Next Priority
1. Implement runtime mode module with permission matrix and tests (F-003).
2. Implement policy validator with allow/deny/protected path checks (F-004).
3. Implement approval persistence manager (F-005).

Completion Snapshot
- 2/14 checklist items passing.
- Baseline/bootstrapping complete and ready for coding agent handoff.

---

Session Update - 2026-02-14 (Coding Agent)

Completed this session
- Reconstructed context from authoritative artifacts: app_spec.txt, feature_list.json, README.md, claude-progress.txt, git history.
- Ran environment bootstrap and regression checks:
  - powershell -ExecutionPolicy Bypass -File ./init.ps1
  - cargo run -- --mode edit
  - cargo run -- --mode plan
- Implemented runtime mode module for plan/edit/auto in `src/modes/runtime_mode.rs` with:
  - capability matrix (`can_write`, `can_execute`, `requires_approval`)
  - normalized string conversion and display
  - strict mode parsing with explicit validation error
- Integrated runtime mode into CLI startup path in `src/main.rs`.
- Added/ran tests for permission matrix and parsing/round-trip behavior.

Verified checklist items
- F-003 passed with command and test evidence recorded in feature_list.json.

Issues found and fixed
- No regression found in selected core flows (bootstrap and mode startup).
- Added explicit invalid mode handling (`invalid runtime mode ... expected one of: plan, edit, auto`) to prevent silent misconfiguration.

Next Priority
1. Implement policy validator with allow/deny/protected path checks (F-004).
2. Implement approval persistence manager (F-005).
3. Implement session manager create/list/load/switch flow (F-006).

Completion Snapshot
- 3/14 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 2)

Completed this session
- Reconstructed session context from authoritative artifacts and confirmed repository state before coding.
- Re-ran environment/bootstrap + regression flows:
  - powershell -ExecutionPolicy Bypass -File ./init.ps1
  - cargo run -- --mode edit
  - cargo run -- --mode plan
- Implemented policy validation module in `src/policy/validator.rs`:
  - JSON-loadable policy config (`allow_command_prefixes`, `deny_command_patterns`, `protected_paths`)
  - command validation with explicit deny/allow-list failures
  - protected path blocking with explicit path-level error
- Exported policy module from `src/lib.rs` via `pub mod policy`.
- Added policy unit tests for required F-004 verification scenarios.

Verified checklist items
- F-004 passed with test + command evidence recorded in `feature_list.json`.

Issues found and fixed
- No functional regression found in sampled core flows (bootstrap and runtime mode startup).

Next Priority
1. Implement approval persistence manager (F-005).
2. Implement session manager create/list/load/switch flow (F-006).
3. Implement tool registry and normalized envelopes (F-007).

Completion Snapshot
- 4/14 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 3)

Completed this session
- Reconstructed context from authoritative artifacts and verified current repository state before coding.
- Ran environment/bootstrap + regression pre-checks:
  - powershell -ExecutionPolicy Bypass -File ./init.ps1
  - cargo run -- --mode edit
  - cargo run -- --mode plan
  - cargo test allows_configured_safe_command
  - cargo test rejects_denied_command_with_explicit_error
- Implemented approval persistence module for P0 F-005:
  - Added `src/approvals/manager.rs` and `src/approvals/mod.rs`.
  - Added `ApprovalType` for `allow_once` / `allow_session` / `allow_global`.
  - Added JSON persistence with `load_or_create` and `save`.
  - Added scope-aware lookup and `allow_once` consumption via `authorize_command`.
- Exported approvals module from `src/lib.rs`.
- Ran formatting and full regression:
  - cargo fmt
  - cargo test
  - cargo run -- --mode edit

Verified checklist items
- F-005 passed with implementation + unit test evidence recorded in `feature_list.json`.

Issues found and fixed
- Initial targeted cargo test syntax was invalid for multi-name filter; corrected to explicit per-test invocations.
- `cargo fmt --check` reported repository formatting drift in `src/policy/validator.rs`; normalized with `cargo fmt`.
- Removed an unused import warning in approvals module to keep build output clean.

Next Priority
1. Implement session manager create/list/load/switch flow (F-006).
2. Implement tool registry and normalized envelopes (F-007).
3. Start minimal TUI shell structure for status/message/input areas (F-008).

Completion Snapshot
- 5/14 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 4)

Completed this session
- Reconstructed context from authoritative artifacts (`app_spec.txt`, `feature_list.json`, `README.md`, `claude-progress.txt`) and checked recent git history.
- Ran bootstrap and regression pre-checks before new feature work:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `cargo run -- --mode plan`
- Implemented P0 session manager (`F-006`):
  - Added `src/session/manager.rs` and `src/session/mod.rs`.
  - Added `Session` / `SessionMeta` models and `SessionManager` with `create`, `list`, `load`, `switch`, `save_session`, `current_session`.
  - Added default config-path constructor (`with_default_dir`) and input validation for session id/name.
- Exported session module through `src/lib.rs`.
- Added dependencies required for session persistence path and stable ids: `dirs`, `uuid`.
- Added and executed session tests for required checklist workflow:
  - create two sessions + persist + list metadata
  - load selected session and switch active context
  - invalid session id rejection

Verified checklist items
- F-006 passed with implementation + unit/integration evidence recorded in `feature_list.json`.

Issues found and fixed
- No functional regression found in sampled core flows after session module integration.
- During parallel regression command execution, cargo reported expected transient file-lock waits; all commands completed successfully without errors.

Next Priority
1. Implement tool registry and normalized result envelopes (F-007).
2. Implement minimal TUI shell with status/message/input and key handling (F-008).
3. Implement streaming output delivery to UI loop (F-009).

Completion Snapshot
- 6/14 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 5)

Completed this session
- Reconstructed context from authoritative artifacts and repository state:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Re-ran startup and regression pre-checks before new implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `cargo run -- --mode plan`
- Implemented P0 tool system (`F-007`) with a common interface and normalized result envelope:
  - Added `src/tools/registry.rs` with `Tool`, `ToolDefinition`, `ToolContext`, `ToolRegistry`, `ToolResultEnvelope`.
  - Added `src/tools/builtin.rs` with built-in `shell`, `file`, `git` tools and `register_builtin_tools`.
  - Added `src/tools/mod.rs` and exported module in `src/lib.rs`.
- Added unit coverage for full registry workflow:
  - `tools::builtin::tests::registry_executes_builtin_tools_with_normalized_envelopes`
  - Valid + invalid argument cases for shell/file/git.
  - Unknown tool (`tool_not_found`) envelope assertion.
- Ran formatting and full regression:
  - `cargo fmt`
  - `cargo test`
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `cargo run -- --mode plan`

Verified checklist items
- F-007 passed with implementation + end-to-end unit validation evidence recorded in `feature_list.json`.

Issues found and fixed
- No regressions found in sampled core flows.
- Observed transient cargo file-lock wait logs during parallel command execution; all commands completed successfully.

Next Priority
1. Implement minimal TUI shell with status/message/input and key handling (F-008).
2. Implement streaming agent output delivery to UI loop without blocking input (F-009).
3. Start audit logging for tool invocation records (F-010).

Completion Snapshot
- 7/14 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 6)

Completed this session
- Reconstructed context from authoritative artifacts (`app_spec.txt`, `feature_list.json`, `README.md`, `claude-progress.txt`) and verified repo status/history.
- Ran bootstrap and regression pre-checks before new work:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `cargo run -- --mode plan`
- Implemented P0 TUI shell (`F-008`):
  - Added `src/tui/mod.rs` with `App` state model and `draw` renderer for status bar, message area, input box.
  - Added keyboard handling for type, backspace, submit (`Enter`), message scrolling (`Up`/`Down`), and quit (`q`).
  - Added async-like processing state transition (`processing -> idle`) with assistant reply append.
  - Added optional scripted key injection via `FASTCODE_TUI_SCRIPT` for deterministic end-to-end UI verification in CI/non-interactive sessions.
- Wired CLI to launch UI with `--tui` while keeping default bootstrap path backward compatible.
- Updated README with TUI launch command and controls.
- Ran formatting and full regression:
  - `cargo fmt`
  - `cargo test` (22 passed, 0 failed)
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,Up,Down,q'; cargo run -- --mode edit --tui`

Verified checklist items
- F-008 passed with implementation + keyboard workflow + render verification evidence recorded in `feature_list.json`.

Issues found and fixed
- No regressions found in sampled core flows after TUI integration.
- Preserved previous non-TUI startup behavior to avoid breaking existing F-002 verification path.

Next Priority
1. Implement streaming agent output delivery without blocking input loop (F-009).
2. Implement audit logger with JSONL records for tool invocation (F-010).
3. Validate non-functional handoff-audit scenario as a concrete reproducibility check (NF-001).

Completion Snapshot
- 8/14 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 7)

Completed this session
- Reconstructed full session context from authoritative artifacts and git history before coding.
- Ran bootstrap and pre-development regression checks:
  - `./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,Up,Down,q'; cargo run -- --mode edit --tui`
- Implemented P0 streaming UI behavior for `F-009` in `src/tui/mod.rs`:
  - Replaced single-shot delayed reply with incremental chunked streaming using `StreamState`, `chunk_text`, and tick-driven appends.
  - Added scripted action support for `sleepNN`/`waitNN` in `FASTCODE_TUI_SCRIPT` to validate multi-step stream rendering in real UI flow.
  - Kept input loop non-blocking during processing (typing remains available; only submit is gated while processing).
- Added/updated tests for streaming and non-blocking interaction:
  - `processing_transitions_back_to_idle_and_appends_assistant_reply`
  - `streaming_does_not_block_user_typing_during_processing`
  - `parses_scripted_key_sequence_tokens`
  - `chunking_keeps_stream_order_stable`
- Executed verification:
  - `cargo fmt`
  - `cargo test` (24 passed, 0 failed)
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,sleep80,sleep80,sleep80,Down,q'; cargo run -- --mode edit --tui`
  - Observed terminal output evidence of incremental assistant stream chunks (`rece` -> `ived` -> `hell` -> `o`) and final status transition `processing -> idle`.

Verified checklist items
- F-009 passed with implementation + tests + real scripted UI evidence recorded in `feature_list.json`.

Issues found and fixed
- Validation gap: previous scripted TUI flow could not model timed waits for stream observation.
- Fix: added scripted sleep actions to support deterministic end-to-end streaming verification without bypassing UI loop.

Next Priority
1. Validate and close `NF-001` by recording explicit cross-session reconstruction evidence in checklist.
2. Implement audit logger with JSONL invocation records (`F-010`).
3. Implement safety-default verification path for denied commands via tool interface (`NF-003`).

Completion Snapshot
- 9/14 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 8)

Completed this session
- Reconstructed context from authoritative handoff artifacts and repository history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Ran environment bootstrap and regression checks before advancing checklist status:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`
- Computed pending checklist state from `feature_list.json` to validate deterministic priority selection:
  - `pending_count=5`
  - `pending=NF-001 priority=P0`
  - `pending=F-010 priority=P1`
  - `pending=NF-002 priority=P1`
  - `pending=NF-003 priority=P1`
  - `pending=NF-004 priority=P1`
- Updated tracker status for `NF-001` with concrete reconstruction + regression evidence.

Verified checklist items
- `NF-001` passed with auditable cross-session reconstruction evidence and explicit next-priority derivation.

Issues found and fixed
- No regressions detected in sampled core startup and TUI interaction flows.

Next Priority
1. Implement audit logger writing JSONL tool invocation records (`F-010`).
2. Verify safety-default denied-command behavior through tool interface (`NF-003`).
3. Capture cold-start timing baseline (`NF-002`).

Completion Snapshot
- 10/14 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 9)

Completed this session
- Reconstructed context from authoritative artifacts and verified current git/worktree state before implementation.
- Ran environment startup and pre-change regression checks:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`
- Implemented P1 audit logging workflow (`F-010`):
  - Added `src/audit/mod.rs` and `src/audit/logger.rs` with JSONL append logger and `AuditRecord` fields: `timestamp_unix_ms`, `mode`, `tool`, `result`, `error_code`, `error_message`.
  - Exported audit module via `src/lib.rs`.
  - Added `ToolRegistry::execute_with_audit` in `src/tools/registry.rs` to keep existing execution behavior while logging mode + result for each invocation.
- Added verification tests for required checklist behavior:
  - `audit::logger::tests::appends_parseable_jsonl_records_with_required_fields`
  - `tools::builtin::tests::writes_audit_records_for_tool_invocations_across_modes`
- Ran post-change validation:
  - `cargo fmt`
  - `cargo test` (26 passed, 0 failed)
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,Down,q'; cargo run -- --mode edit --tui`

Verified checklist items
- `F-010` passed with implementation + parseable JSONL audit evidence + cross-mode tool invocation verification.

Issues found and fixed
- No regressions detected in sampled core CLI/TUI workflows after audit integration.

Next Priority
1. Verify safety-default denied-command behavior through tool interface (`NF-003`).
2. Capture and record cold-start build timing baseline (`NF-002`).
3. Validate terminal UX behavior under 80x24 and 120x40 sizes (`NF-004`).

Completion Snapshot
- 11/14 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 10)

Completed this session
- Reconstructed context from authoritative artifacts and verified repository/git status in fresh window.
- Ran startup/bootstrap and regression pre-checks before new work:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`
- Implemented safety-default tool guardrails for `NF-003`:
  - Added `ToolRegistry::execute_with_safety` in `src/tools/registry.rs`.
  - Enforced shell pre-execution checks for runtime mode (`can_execute`), policy deny validation, and edit-mode approval requirements.
  - Added actionable user-facing resolution text for denied/approval-required responses.
- Added end-to-end safety verification tests in `src/tools/builtin.rs`:
  - `blocks_denied_shell_command_before_execution_with_resolution_error`
  - `blocks_shell_command_without_required_approval_in_edit_mode`
- Ran post-change validation:
  - `cargo fmt`
  - `cargo test` (28 passed, 0 failed)
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,Down,q'; cargo run -- --mode edit --tui`
  - `cargo test blocks_denied_shell_command_before_execution_with_resolution_error`
  - `cargo test blocks_shell_command_without_required_approval_in_edit_mode`

Verified checklist items
- `NF-003` passed with blocked high-risk command evidence, blocked-before-execution file-preservation assertion, and approval-required user-facing resolution verification.

Issues found and fixed
- Initial targeted cargo test command incorrectly provided two test filters in one invocation; corrected to separate test commands.
- No functional regression detected in sampled CLI/TUI core flows after safety guard integration.

Next Priority
1. Capture and record cold-start build timing baseline (`NF-002`).
2. Validate terminal UX at 80x24 and 120x40 sizes (`NF-004`).

Completion Snapshot
- 12/14 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 11)

Completed this session
- Reconstructed context from authoritative artifacts and git history:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Ran environment/bootstrap and regression pre-checks before new implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`
- Implemented terminal-size robustness for `NF-004` in `src/tui/mod.rs`:
  - Added explicit resize event handling (`Event::Resize`) and `App::on_resize`.
  - Added scripted resize actions (`resizeWxH` / `sizeWxH`) for deterministic UI verification.
  - Added optional startup size parser (`FASTCODE_TUI_SIZE`) and viewport size telemetry in status bar (`size: WxH`).
  - Kept input/scroll/submit behavior intact during and after resize transitions.
- Added and executed tests for standard terminal-size behavior and post-resize usability:
  - `tui::tests::draw_supports_standard_terminal_sizes`
  - `tui::tests::resize_keeps_input_and_navigation_functional`
  - `tui::tests::parses_terminal_size_token`
- Ran post-change validation:
  - `cargo fmt`
  - `cargo test` (31 passed, 0 failed)
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,Up,Down,resize120x40,w,o,r,l,d,Enter,sleep80,sleep80,q'; cargo run -- --mode edit --tui`
  - `$env:FASTCODE_TUI_SCRIPT='t,e,s,t,Enter,sleep80,sleep80,resize80x24,Up,Down,b,a,c,k,Enter,sleep80,sleep80,q'; cargo run -- --mode edit --tui`

Verified checklist items
- `NF-004` passed with implementation + tests + scripted real UI path evidence recorded in `feature_list.json`.

Issues found and fixed
- Gap: terminal resize events were previously ignored in runtime loop, making resize behavior unverifiable and non-explicit.
- Fix: handled resize events directly and introduced deterministic scripted resize actions to preserve an auditable, repeatable verification path.

Next Priority
1. Capture and record cold-start build timing baseline (`NF-002`).

Completion Snapshot
- 13/14 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 12)

Completed this session
- Reconstructed context from authoritative artifacts and verified repository/git status in a fresh window.
- Ran environment/bootstrap and regression pre-checks before new work:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`
- Implemented cold-start baseline tracking workflow for `NF-002`:
  - Added `scripts/measure-cold-start.ps1` to enforce clean-target timing, JSONL persistence, and previous-run delta reporting.
  - Added `docs/cold-start-baseline.md` and baseline data file `docs/cold-start-baseline.jsonl` for auditable trend tracking.
  - Updated `README.md` with the baseline capture command.
- Captured and compared clean-build timings using real runs:
  - `powershell -ExecutionPolicy Bypass -File ./scripts/measure-cold-start.ps1` -> `elapsed_seconds: 12.326` (first baseline)
  - `powershell -ExecutionPolicy Bypass -File ./scripts/measure-cold-start.ps1` -> `elapsed_seconds: 12.58`, `delta_seconds: 0.254 (2.06%, slower)`
- Ran post-change sanity regression:
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='t,e,s,t,Enter,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`

Verified checklist items
- `NF-002` passed with clean build timing evidence and previous-baseline comparison recorded in `feature_list.json`.

Issues found and fixed
- Script bootstrap issue: initial `measure-cold-start.ps1` placed `param(...)` below another statement, which PowerShell rejects.
- Fix: moved `param(...)` to file header and re-ran measurements successfully.

Next Priority
1. All current checklist items are passing; next step is to expand feature list for next milestone (MCP integration, memory/rollback, or plan-mode expansion per app_spec scope).

Completion Snapshot
- 14/14 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 13)

Completed this session
- Reconstructed context from authoritative artifacts and repo history in fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Confirmed current checklist state and gap:
  - `total=14`
  - `passing=14`
  - `pending=0`
- Ran environment bootstrap and pre-change regression checks before new feature work:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`
- Expanded checklist for next milestone scope from `app_spec.txt` and completed one P1 item end-to-end:
  - Added `F-011` (MCP config load/list validation workflow).
  - Implemented MCP config loader + validation in `src/mcp/config.rs` and exported module in `src/lib.rs`.
  - Added CLI integration in `src/main.rs` with `--mcp-config` + `--list-mcp-servers`.
  - Added `docs/mcp-servers.example.json` and README usage/troubleshooting notes.

Verified checklist items
- `F-011` passed with:
  - Successful CLI path: `cargo run -- --mode edit --list-mcp-servers --mcp-config ./docs/mcp-servers.example.json` (loaded 2 servers).
  - Failure path: `cargo run -- --mode edit --list-mcp-servers` (actionable missing-config error, exit code 1).
  - Unit tests for valid/invalid MCP config loading plus full suite: `cargo test` (34 passed, 0 failed).

Issues found and fixed
- No regressions found in sampled core CLI and TUI workflows after MCP changes.

Next Priority
1. Add runtime MCP process lifecycle management (spawn/health/check/shutdown) with audit hooks.
2. Integrate MCP-backed tools into `ToolRegistry` with policy + approval compatibility checks.
3. Add scripted TUI flow for MCP server selection/status visibility.

Completion Snapshot
- 15/15 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 14)

Completed this session
- Reconstructed context from authoritative artifacts and repo history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Verified repository state and checklist statistics before coding:
  - `total=15`
  - `pending=0`
- Ran environment startup and regression pre-checks before new feature work:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`
- Added new high-priority unfinished item from app_spec follow-up scope and completed it end-to-end:
  - Added `F-012` (MCP process lifecycle management).
  - Implemented `src/mcp/lifecycle.rs` with `McpProcessManager` and lifecycle APIs:
    - process spawn: `start_server`, `start_all`
    - health checks: `health` (running/exited)
    - clean shutdown: `shutdown_server`, `shutdown_all`
    - operator visibility: `managed_names`
  - Exported module via `src/mcp/mod.rs`.

Verified checklist items
- `F-012` passed with:
  - Targeted lifecycle tests: `cargo test mcp::lifecycle` (3 passed, 0 failed).
  - Full regression: `cargo test` (37 passed, 0 failed).
  - Real user-path UI regression: `cargo run -- --mode edit` and scripted TUI interaction flow (input/submit/scroll/quit) exited 0.

Issues found and fixed
- No functional or UI regression detected in sampled core flows.
- Observed transient cargo file-lock wait logs during parallel command execution; commands completed successfully.

Next Priority
1. Integrate MCP lifecycle manager into CLI workflow (start/health/shutdown command path with explicit errors).
2. Add audit hooks for MCP lifecycle events to align with auditable multi-session operations.
3. Expose MCP server runtime status in TUI status area for operator-visible diagnostics.

Completion Snapshot
- 16/16 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 15)

Completed this session
- Reconstructed context from authoritative artifacts and repository state in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Ran bootstrap and pre-development regression checks:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`
- Added and completed new high-priority unfinished checklist item `F-013` (MCP lifecycle CLI workflow):
  - Added lifecycle report helper `run_lifecycle_check` and `McpLifecycleReport` in `src/mcp/lifecycle.rs`.
  - Added CLI flag `--check-mcp-lifecycle` in `src/main.rs`.
  - Implemented explicit workflow output (`started`/`health`/`shutdown`) and actionable missing-config error.
  - Added lifecycle report test `mcp::lifecycle::tests::lifecycle_check_reports_start_health_and_shutdown_counts`.
- Updated docs and tracker artifacts:
  - `README.md` updated with lifecycle command usage and troubleshooting note.
  - `feature_list.json` extended with `F-013` and evidence.

Verified checklist items
- `F-013` passed with:
  - Success path: `cargo run -- --mode edit --check-mcp-lifecycle --mcp-config <temp-config>` showed start/health/shutdown and exited 0.
  - Failure path: `cargo run -- --mode edit --check-mcp-lifecycle` failed with explicit `--check-mcp-lifecycle requires --mcp-config <path-to-json-config>`.
  - Tests: `cargo test` passed full suite (38 passed, 0 failed).

Issues found and fixed
- PowerShell quoting error while updating `feature_list.json` caused an initial write failure.
- Fixed by switching to a script-block update path and reapplying the change successfully.
- No functional/UI regressions detected in sampled CLI and TUI core flows after MCP lifecycle CLI integration.

Next Priority
1. Add audit hooks for MCP lifecycle events to align process operations with existing JSONL audit trail.
2. Expose MCP runtime status in TUI status/message area for operator-visible diagnostics.
3. Add CLI subcommand structure to avoid flag-combination ambiguity as MCP workflows expand.

Completion Snapshot
- 17/17 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 16)

Completed this session
- Reconstructed context from authoritative artifacts and verified repo status/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Verified checklist state before work: `pending=0`; expanded tracker with the next high-priority item from prior handoff and completed it end-to-end.
- Ran environment startup and regression pre-checks before implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`
- Implemented MCP lifecycle audit integration (`F-014`):
  - Added `run_lifecycle_check_with_audit` in `src/mcp/lifecycle.rs`.
  - Added stage-level audit hooks for `start`/`health`/`shutdown` while preserving existing `run_lifecycle_check` behavior.
  - Added CLI support in `src/main.rs` for optional `--audit-log <path>` with `--check-mcp-lifecycle`.
  - Updated `README.md` with lifecycle audit command and troubleshooting guidance.
- Added/ran tests and validation:
  - Added `mcp::lifecycle::tests::lifecycle_check_writes_audit_events_for_start_health_and_shutdown`.
  - `cargo fmt`
  - `cargo test` (39 passed, 0 failed)
  - `cargo run -- --mode edit --check-mcp-lifecycle --mcp-config target/mcp-lifecycle-test.json --audit-log target/mcp-lifecycle-audit.jsonl`
  - `cargo run -- --mode edit --check-mcp-lifecycle` (expected explicit error).

Verified checklist items
- `F-014` passed with implementation + parseable JSONL evidence recorded in `feature_list.json`.

Issues found and fixed
- One aggregated PowerShell verification command was blocked by local shell policy due restricted token usage.
- Fixed by switching to conservative split commands; validation completed successfully.
- No functional or UI regressions found in sampled CLI/TUI core flows after MCP audit integration.

Next Priority
1. Expose MCP runtime status in TUI status/message area for operator-visible diagnostics.
2. Add CLI subcommand structure to reduce flag-combination ambiguity as MCP workflows expand.
3. Expand MCP lifecycle failure telemetry (startup/shutdown error events) into user-facing diagnostics.

Completion Snapshot
- 18/18 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 17)

Completed this session
- Reconstructed context from authoritative artifacts and repository history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Verified startup + regression guardrails before new feature work:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`
- Expanded tracker with a new high-priority unfinished item and closed it end-to-end:
  - Added `F-015`: TUI MCP runtime diagnostics visibility.
  - Updated `src/tui/mod.rs` with `McpDiagnostics`, `App::new_with_mcp_diagnostics`, and `run_app_with_mcp_diagnostics`.
  - Extended status bar to render `mcp: <state>` and message area to include diagnostics lines.
  - Updated `src/main.rs` to derive MCP diagnostics from `--mcp-config` via lifecycle health probe before entering TUI.
- Updated docs:
  - Added TUI MCP diagnostics run command and troubleshooting note in `README.md`.

Verified checklist items
- `F-015` passed with:
  - `cargo test` (40 passed, 0 failed), including new TUI diagnostics test.
  - Real UI path with MCP config:
    - `$env:FASTCODE_TUI_SCRIPT='h,i,Enter,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config target/mcp-tui-test.json`
    - Status bar showed `mcp: ok 1/1`; input/submit/scroll flow remained functional.
  - Invalid-config visibility path:
    - launching TUI with malformed config showed `mcp: invalid-config` and explicit parse error in message area.

Issues found and fixed
- Initial temp MCP config write used an encoding path that produced parse failure during TUI diagnostics verification.
- Fixed by rewriting the temp config as plain ASCII JSON and re-running the real UI flow successfully.
- No core regression found in sampled CLI/TUI workflows after integration.

Next Priority
1. Add CLI subcommand structure to reduce flag-combination ambiguity as MCP workflows expand.
2. Expand MCP lifecycle failure telemetry (startup/shutdown errors) into user-facing diagnostics and audit records.
3. Expose live MCP status refresh inside TUI loop (not only launch-time diagnostics).

Completion Snapshot
- 19/19 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 18)

Completed this session
- Reconstructed context from authoritative artifacts and repository status in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Ran environment/bootstrap and regression pre-checks before new feature work:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`
- Added and closed new high-priority unfinished item:
  - Added `F-016`: MCP CLI subcommand structure with ambiguity guardrails.
  - Refactored `src/main.rs` to support `mcp list` / `mcp check-lifecycle` while keeping legacy MCP flags compatible.
  - Added explicit conflict handling for mixed/ambiguous MCP action inputs.
- Updated docs:
  - Updated MCP command examples in `README.md` to use subcommands.

Verified checklist items
- `F-016` passed with:
  - Unit verification: `cargo test` passed including new CLI routing tests in `src/main.rs` (3/3 for binary tests; full suite passed).
  - New subcommand real-path checks:
    - `cargo run -- --mode edit mcp list --mcp-config ./docs/mcp-servers.example.json`
    - `cargo run -- --mode edit mcp check-lifecycle --mcp-config target/mcp-cli-subcommand-test.json --audit-log target/mcp-cli-subcommand-audit.jsonl`
  - Ambiguity/error-path checks:
    - `cargo run -- --mode edit --list-mcp-servers --check-mcp-lifecycle --mcp-config ./docs/mcp-servers.example.json` -> explicit single-action error.
    - `cargo run -- --mode edit --list-mcp-servers --mcp-config ./docs/mcp-servers.example.json mcp list --mcp-config ./docs/mcp-servers.example.json` -> explicit mixed legacy/subcommand error.

Issues found and fixed
- `src/main.rs` incremental patching failed due context drift; resolved by rewriting file once with consolidated changes and re-running format/tests.
- No functional/UI regression found in sampled CLI/TUI core flows after CLI refactor.

Next Priority
1. Expand MCP lifecycle failure telemetry (startup/shutdown errors) into richer user-facing diagnostics and audit events.
2. Add live MCP status refresh in TUI loop instead of launch-time-only diagnostics.
3. Consider deprecation timeline messaging for legacy MCP flags once downstream scripts migrate.

Completion Snapshot
- 20/20 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 19)

Completed this session
- Reconstructed context from authoritative artifacts and repository state in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Ran environment/bootstrap and regression pre-checks before new work:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`
- Added and completed new high-priority unfinished item:
  - Added `F-017`: MCP lifecycle start-failure telemetry and cleanup auditability.
  - Updated `src/mcp/lifecycle.rs` to use incremental server startup, emit start-error audit records (`mcp_start_failed`), and force shutdown cleanup for already-started servers before returning failure.
  - Added shutdown-error audit emission for failed teardown paths.
- Updated docs/tracking artifacts:
  - `README.md` troubleshooting note for partial startup cleanup + audit event inspection.
  - `feature_list.json` extended with `F-017` and evidence.

Verified checklist items
- `F-017` passed with:
  - `cargo test mcp::lifecycle` (6 passed, 0 failed).
  - `cargo test` full suite (41 passed, 0 failed).
  - Real failure-path run:
    - `cargo run -- --mode edit mcp check-lifecycle --mcp-config target/mcp-lifecycle-failure-test.json --audit-log target/mcp-lifecycle-failure-audit.jsonl`
    - CLI returned explicit error including failed server and cleanup counts (`started 1 ... stopped 1`).
    - Audit JSONL recorded `mcp_lifecycle:start:sleepy` success, `mcp_lifecycle:start:broken` error (`mcp_start_failed`), and `mcp_lifecycle:shutdown:sleepy` success.
- Post-change regression recheck passed:
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,i,Enter,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`

Issues found and fixed
- Gap: lifecycle startup failure previously aborted immediately on `start_all` error, with limited telemetry and no explicit partial-start cleanup reporting.
- Fix: staged startup + cleanup with explicit error payload and audit events for failed server startup and cleanup stage.

Next Priority
1. Add live MCP status refresh inside the TUI loop (currently diagnostics are launch-time snapshot).
2. Expand MCP lifecycle shutdown-failure simulation tests to assert manager-level error telemetry under forced kill/wait failures.
3. Consider deprecation messaging timeline for legacy MCP flags once downstream scripts migrate to subcommands.

Completion Snapshot
- 21/21 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 20)

Completed this session
- Reconstructed context from authoritative artifacts and repository status/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Ran environment/bootstrap and regression pre-checks before new work:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep80,sleep80,Up,Down,q'; cargo run -- --mode edit --tui`
- Added and completed new high-priority unfinished item:
  - Added `F-018`: live MCP diagnostics refresh inside TUI runtime.
  - Updated `src/tui/mod.rs` to refresh MCP diagnostics every 800ms when `--mcp-config` is present, update status indicator with refresh counter (`mcp: <state> rN`), and append refresh summary messages.
  - Updated `src/main.rs` to pass `--mcp-config` into TUI runtime refresh path.
- Updated docs/tracking artifacts:
  - `README.md` updated to document continuous MCP refresh behavior in TUI.
  - `feature_list.json` extended with `F-018` and verification evidence.

Verified checklist items
- `F-018` passed with:
  - `cargo test` full suite (42 passed, 0 failed).
  - Real UI path: `$env:FASTCODE_TUI_SCRIPT='h,i,Enter,sleep900,sleep900,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config target/mcp-tui-refresh-test.json`.
  - Terminal output showed live status updates `mcp: ok 1/1 r0 -> r1 -> r2 -> r3` during interaction, with input/submit/scroll behavior intact.

Issues found and fixed
- `feature_list.json` parser assumption mismatch during context restore (`features` object vs top-level array) caused an initial incorrect pending-count read.
- Fixed by switching tracker-stat commands to array-based parsing and proceeding with auditable item expansion.
- No functional or UI regression found in sampled CLI/TUI core flows after live refresh integration.

Next Priority
1. Expand MCP lifecycle shutdown-failure simulation tests to assert manager-level telemetry under forced kill/wait failures.
2. Consider deprecation timeline messaging for legacy MCP flags once downstream scripts are migrated to subcommands.
3. Add optional MCP refresh interval configuration (CLI/env) for operators who need lower runtime overhead.

Completion Snapshot
- 22/22 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 21)

Completed this session
- Reconstructed context from authoritative artifacts and repository history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Confirmed baseline tracker state before work: all existing items were already passing (22/22), then added and closed one new high-priority unfinished item in this session.
- Ran environment/bootstrap and regression pre-checks before implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep120,sleep120,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config target/mcp-tui-refresh-test.json`
- Added and completed new high-priority unfinished item:
  - Added `F-019`: configurable MCP diagnostics refresh interval in TUI.
  - Updated `src/main.rs` with CLI flag `--mcp-refresh-ms` (validated as positive integer) and wired value into TUI runtime.
  - Updated `src/tui/mod.rs` to remove hardcoded MCP refresh interval from loop logic and use runtime-configured interval with default fallback `DEFAULT_MCP_REFRESH_INTERVAL_MS=800`.
  - Added CLI parsing tests in `src/main.rs` for valid and invalid interval values.
- Updated docs/tracking artifacts:
  - `README.md` updated with `--mcp-refresh-ms` usage example.
  - `feature_list.json` extended with `F-019` and verification evidence.

Verified checklist items
- `F-019` passed with:
  - `cargo fmt`
  - `cargo test` (42 library tests + 5 binary tests, 0 failed)
  - Real UI path: `$env:FASTCODE_TUI_SCRIPT='h,i,Enter,sleep260,sleep260,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config target/mcp-tui-refresh-test.json --mcp-refresh-ms 200`
  - TUI evidence showed accelerated refresh progression in status line (`mcp: ok 1/1 r1 -> r2 -> r3`) while input/submit/scroll behavior remained intact.
  - Error-path CLI verification: `cargo run -- --mode edit --tui --mcp-refresh-ms 0` returned explicit clap range error.

Issues found and fixed
- No regressions found in sampled CLI/TUI core flows after introducing configurable MCP refresh interval.
- One exploratory full-tree listing command produced excessive output; no functional impact.

Next Priority
1. Expand MCP lifecycle shutdown-failure simulation tests to assert manager-level telemetry under forced kill/wait failures.
2. Consider deprecation timeline messaging for legacy MCP flags as downstream scripts migrate.
3. Add optional environment-based MCP refresh override if operators need policy-driven defaults across sessions.

Completion Snapshot
- 23/23 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 22)

Completed this session
- Reconstructed context from authoritative artifacts and repository history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Ran startup/bootstrap and regression pre-checks before new work:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep260,sleep260,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config target/mcp-tui-refresh-test.json --mcp-refresh-ms 200`
- Added and completed new high-priority unfinished item:
  - Added `F-020`: environment-variable MCP refresh interval for TUI (`FASTCODE_MCP_REFRESH_MS`) with precedence `--mcp-refresh-ms` > env > default.
  - Updated `src/main.rs` with explicit env parsing/validation path and added tests for env resolution, CLI-over-env precedence, invalid value rejection, and zero rejection.
- Updated docs/tracking artifacts:
  - `README.md` updated with `FASTCODE_MCP_REFRESH_MS` usage.
  - `feature_list.json` extended with `F-020` and verification evidence.

Verified checklist items
- `F-020` passed with:
  - `cargo fmt`
  - `cargo test` (42 library tests + 9 binary tests, 0 failed)
  - Real UI flow using env-only interval override:
    - `$env:FASTCODE_MCP_REFRESH_MS='200'; $env:FASTCODE_TUI_SCRIPT='h,i,Enter,sleep260,sleep260,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config target/mcp-tui-refresh-test.json`
    - Status counter advanced `mcp: ok 1/1 r1 -> r2 -> r3` while input/submit/scroll remained functional.
  - Error-path verification:
    - `$env:FASTCODE_MCP_REFRESH_MS='bad'; cargo run -- --mode edit --tui` returned non-zero with explicit parsing error.

Issues found and fixed
- No functional/UI regression found in sampled CLI and TUI core workflows after introducing environment-based refresh configuration.

Next Priority
1. Expand MCP lifecycle shutdown-failure simulation tests to assert manager-level telemetry under forced kill/wait failures.
2. Consider deprecation timeline messaging for legacy MCP flags as downstream scripts migrate.
3. Add policy-level bounds for refresh interval (for operator safety/performance governance).

Completion Snapshot
- 24/24 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 23)

Completed this session
- Reconstructed context from authoritative artifacts and repository history/state in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Confirmed tracker baseline before new work: all existing items were passing (24/24); added and completed one new high-priority unfinished item in this session.
- Ran startup/bootstrap and regression pre-checks before implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep260,sleep260,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config ./docs/mcp-servers.example.json --mcp-refresh-ms 200`
- Added and completed new high-priority unfinished item:
  - Added `F-021`: policy-safe MCP refresh interval bounds for both CLI and environment paths.
  - Updated `src/main.rs` to enforce shared bounds `50..=60000` milliseconds for `--mcp-refresh-ms` and `FASTCODE_MCP_REFRESH_MS`.
  - Added/updated binary tests for out-of-range validation:
    - `rejects_too_large_mcp_refresh_interval`
    - `rejects_out_of_range_env_mcp_refresh_interval`
    - adjusted invalid-env error assertion text to range-aware messaging.
- Updated docs/tracking artifacts:
  - `README.md` now documents MCP refresh interval range requirements.
  - `feature_list.json` extended with `F-021` and verification evidence.

Verified checklist items
- `F-021` passed with:
  - `cargo fmt`
  - `cargo test` (42 library tests + 10 binary tests, 0 failed)
  - Real UI path:
    - `$env:FASTCODE_TUI_SCRIPT='h,i,Enter,sleep260,sleep260,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config ./docs/mcp-servers.example.json --mcp-refresh-ms 200`
    - UI flow preserved input/submit/scroll and showed refresh progression (`mcp: error r1 -> r2 -> r3`).
  - Error-path checks:
    - `cargo run -- --mode edit --tui --mcp-refresh-ms 60001` -> non-zero with clap range error (`60001 is not in 50..=60000`).
    - `$env:FASTCODE_MCP_REFRESH_MS='60001'; cargo run -- --mode edit --tui` -> non-zero with `FASTCODE_MCP_REFRESH_MS must be in range 50..=60000 milliseconds`.

Issues found and fixed
- Risk: refresh interval previously only enforced `> 0`, which allowed overly aggressive or overly sparse polling values with potential operator/performance instability.
- Fix: centralized bound enforcement for both configuration channels with explicit and test-covered errors.

Next Priority
1. Expand MCP lifecycle shutdown-failure simulation to assert manager-level telemetry under forced kill/wait failure paths.
2. Consider deprecation timeline messaging for legacy MCP flags as downstream scripts migrate to subcommands.
3. Add truncation/compaction policy for long MCP diagnostic lines in TUI message area to improve readability under narrow terminals.

Completion Snapshot
- 25/25 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 24)

Completed this session
- Reconstructed context from authoritative artifacts and repository status/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Confirmed checklist baseline before implementation: existing items were passing (25/25), then added and closed one new high-priority unfinished item end-to-end in this session.
- Ran environment/bootstrap and regression pre-checks before new work:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep120,resize120x40,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config ./docs/mcp-servers.example.json --mcp-refresh-ms 200`
- Added and completed new high-priority unfinished item:
  - Added `F-022`: TUI MCP diagnostics message compaction for narrow terminal readability.
  - Updated `src/tui/mod.rs` with width-aware `compact_message_for_width` policy and integrated it into both initial diagnostics injection and periodic MCP refresh summary messages.
  - Added tests to verify compaction behavior and short-message pass-through.
- Updated docs/tracking artifacts:
  - `README.md` documented compacted MCP diagnostics marker format (`... (+N chars)`).
  - `feature_list.json` extended with `F-022` and verification evidence.

Verified checklist items
- `F-022` passed with:
  - `cargo fmt`
  - `cargo test` (44 library tests + 10 binary tests, 0 failed)
  - Real UI flow:
    - `$env:FASTCODE_TUI_SCRIPT='resize80x24,sleep260,h,i,Enter,sleep260,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config ./docs/mcp-servers.example.json --mcp-refresh-ms 200`
    - TUI output showed compact diagnostics lines in message area (for example `... (+129 chars)`) at 80x24 while input/submit/scroll and streaming reply remained functional.

Issues found and fixed
- Readability gap: long MCP diagnostics error strings could dominate the message pane under narrow terminal widths.
- Fix: introduced deterministic width-based compaction with explicit truncation indicator and retained full short-message rendering.
- No functional regressions found in sampled CLI/TUI core flows after this change.

Next Priority
1. Add optional user-toggle to expand/collapse compacted MCP diagnostics lines for deep troubleshooting in TUI.
2. Consider moving full untruncated MCP diagnostics payloads to audit log references in UI messages to balance readability and traceability.
3. Evaluate reducing repeated MCP refresh error spam by deduplicating identical consecutive diagnostics summaries.

Completion Snapshot
- 26/26 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 25)

Completed this session
- Reconstructed context from authoritative artifacts and repository state/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Verified startup/bootstrap and regression checks before new implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep260,sleep260,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config ./docs/mcp-servers.example.json --mcp-refresh-ms 200`
- Added and completed one new high-priority unfinished item:
  - Added `F-023`: TUI deduplicates consecutive identical MCP refresh diagnostics summaries while preserving status-bar refresh counter progression.
  - Updated `src/tui/mod.rs` with `last_mcp_refresh_signature` state and duplicate suppression in `App::apply_mcp_refresh`.
  - Added test `tui::tests::deduplicates_identical_consecutive_mcp_refresh_messages`.
- Updated docs/tracking artifacts:
  - `README.md` documents duplicate-refresh suppression behavior.
  - `feature_list.json` extended with `F-023` and verification evidence.

Verified checklist items
- `F-023` passed with:
  - `cargo fmt`
  - `cargo test` (45 library tests + 10 binary tests, 0 failed)
  - Real UI workflow evidence:
    - `cmd /c "set FASTCODE_TUI_SCRIPT=h,i,Enter,sleep260,sleep260,sleep260,Up,Down,q && cargo run -- --mode edit --tui --mcp-config .\\docs\\mcp-servers.example.json --mcp-refresh-ms 200 > target\\f023-tui-dedupe.log 2>&1"`
    - Parsed log `target/f023-tui-dedupe.clean.log` shows `refresh1` with no `refresh2`/`refresh3` entries while status still advanced to `errorr4`.

Issues found and fixed
- Usability issue: under persistent MCP failures, message area repeatedly appended nearly identical refresh diagnostics and drowned user/system messages.
- Fix: consecutive identical `(status_label + summary)` refresh diagnostics are now suppressed; status refresh counter remains accurate.

Next Priority
1. Add optional UI affordance to view full untruncated MCP diagnostics payload for deep troubleshooting while keeping compact default rendering.
2. Add explicit test coverage for dedupe reset when diagnostics summary changes after repeated failures.
3. Evaluate exposing dedupe statistics in status/help text for operator observability.

Completion Snapshot
- 27/27 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 26)

Completed this session
- Reconstructed context from authoritative artifacts and repository state/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Ran environment/bootstrap and regression pre-checks before implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep260,sleep260,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config ./docs/mcp-servers.example.json --mcp-refresh-ms 200`
- Added and completed one new high-priority unfinished item:
  - Added `F-024`: robust MCP refresh dedupe behavior when summaries change and when summary text is missing.
  - Updated `src/tui/mod.rs` `App::apply_mcp_refresh` to generate a fallback diagnostics summary when `diagnostics.messages` is empty, while preserving duplicate suppression for identical signatures.
  - Added tests `tui::tests::appends_new_refresh_message_when_summary_changes` and `tui::tests::appends_fallback_refresh_message_when_summary_is_missing`.
- Updated docs/tracking artifacts:
  - `README.md` documented fallback diagnostics message behavior for empty MCP summary refreshes.
  - `feature_list.json` extended with `F-024` and verification evidence.

Verified checklist items
- `F-024` passed with:
  - `cargo test` (47 library tests + 10 binary tests, 0 failed)
  - Real UI regression flow:
    - `$env:FASTCODE_TUI_SCRIPT='h,i,Enter,sleep260,sleep260,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config ./docs/mcp-servers.example.json --mcp-refresh-ms 200`
    - Confirmed status refresh counter progression and intact input/submit/scroll interactions, exit 0.

Issues found and fixed
- Edge-case gap: when MCP refresh diagnostics had an empty `messages` payload, UI previously advanced refresh counter but emitted no refresh message, reducing operator observability.
- Fix: added deterministic fallback summary text so each refresh cycle can surface a user-visible diagnostics line when needed, while keeping duplicate suppression and compaction intact.

Next Priority
1. Add optional UI affordance to inspect full untruncated MCP diagnostics payload for troubleshooting while keeping compact default rendering.
2. Add focused lifecycle test coverage for MCP shutdown failure telemetry in forced kill/wait error paths.
3. Evaluate exposing refresh-deduplication counters in status/help text for operator observability.

Completion Snapshot
- 28/28 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 27)

Completed this session
- Reconstructed context from authoritative artifacts and repository state/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Confirmed tracker baseline had no pending items (`pending_total=0`), then added and closed one new high-priority unfinished item end-to-end this session.
- Ran startup/bootstrap and regression pre-checks before new implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `$env:FASTCODE_TUI_SCRIPT='h,e,l,l,o,Enter,sleep260,sleep260,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config ./docs/mcp-servers.example.json --mcp-refresh-ms 200`
- Added and completed one new high-priority unfinished item:
  - Added `F-025`: TUI explicit action to inspect full untruncated MCP diagnostics details while keeping compact default message rendering.
  - Updated `src/tui/mod.rs` to track latest raw MCP summary (`last_mcp_summary_raw`) and bind key `v` to append `system: MCP details: ...` in the message area.
  - Updated status-bar affordance text to include `v mcp details`.
  - Added tests for `v` behavior in both normal and no-diagnostics paths.
- Updated docs/tracking artifacts:
  - `README.md` now documents `v` shortcut.
  - `feature_list.json` extended with `F-025` and verification evidence.

Verified checklist items
- `F-025` passed with:
  - `cargo fmt`
  - `cargo test` (49 library tests + 10 binary tests, 0 failed)
  - Real UI workflow evidence:
    - `cmd /c "set FASTCODE_TUI_SCRIPT=resize80x24,sleep260,v,h,i,Enter,sleep260,Up,Down,q && cargo run -- --mode edit --tui --mcp-config .\\docs\\mcp-servers.example.json --mcp-refresh-ms 200 > target\\f025-mcp-details.log 2>&1"`
    - Parsed evidence in `target/f025-mcp-details.clean.log` shows compact MCP refresh marker (`... (+129 chars)`) and a full details line via `v` (`system: MCP details: MCP diagnostics failed: MCP lifecycle start failed ...`) while user interaction (`user: hi`, `assistant: received`) remains intact.

Issues found and fixed
- Observability gap: compacted MCP diagnostics improved readability but made full failure details harder to inspect directly in TUI.
- Fix: added explicit user action (`v`) to append latest full untruncated diagnostics details on demand without changing compact default behavior.

Next Priority
1. Add optional pagination/search for long MCP details messages to improve troubleshooting in very narrow terminal sessions.
2. Add explicit tests for `v` details behavior after consecutive deduplicated refresh cycles with changing summaries.
3. Evaluate a configurable key map/help overlay for TUI shortcuts.

Completion Snapshot
- 29/29 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 28)

Completed this session
- Reconstructed context from authoritative artifacts and repository state/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Ran environment/bootstrap and regression pre-checks before new implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=h,e,l,l,o,Enter,sleep260,sleep260,Up,Down,q && cargo run -- --mode edit --tui --mcp-config .\\docs\\mcp-servers.example.json --mcp-refresh-ms 200 > target\\regression-preflight-tui.log 2>&1"`
- Added and completed one new high-priority unfinished item:
  - Added `F-026`: TUI status bar exposes MCP refresh deduplication suppression count for operator observability.
  - Updated `src/tui/mod.rs` to track `mcp_refresh_dedup_count` and render status as `mcp: <state> rN dN`.
  - Deduplicated refresh paths now increment `dN` whenever repeated `(status + summary)` refresh messages are suppressed.
  - Added test `tui::tests::status_display_tracks_suppressed_refresh_dedup_count` and updated existing MCP status assertions.
- Updated docs/tracking artifacts:
  - `README.md` now explains `mcp: <state> rN dN` (`rN` refresh count, `dN` suppressed duplicate refresh count).
  - `feature_list.json` extended with `F-026` and end-to-end evidence.

Verified checklist items
- `F-026` passed with:
  - `cargo fmt`
  - `cargo test` (50 lib tests + 10 bin tests, 0 failed)
  - Real UI workflow evidence:
    - `cmd /c "set FASTCODE_TUI_SCRIPT=h,i,Enter,sleep260,sleep260,sleep260,sleep260,Up,Down,q && cargo run -- --mode edit --tui --mcp-config .\\docs\\mcp-servers.example.json --mcp-refresh-ms 200 > target\\f026-mcp-dedupe-counter.log 2>&1"`
    - Parsed evidence in `target/f026-mcp-dedupe-counter.clean.log` shows `mcp: error r0 d0` and later `mcp: error r4 d3`, with message area still deduplicated and interaction preserved (`user: hi`, `assistant: received -> hi`).

Issues found and fixed
- Observability gap: duplicate-refresh suppression reduced message spam but operators could not see how much output was being suppressed.
- Fix: added explicit dedup suppression counter in status bar without changing existing dedupe behavior.

Next Priority
1. Add a focused UI test that validates dedup counter reset semantics if MCP status transitions from repeated failure to healthy summaries.
2. Evaluate exposing dedupe counter in a help line/history message for easier extraction from ANSI logs.
3. Consider optional diagnostics pane pagination/search for long full-detail MCP messages.

Completion Snapshot
- 30/30 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 29)

Completed this session
- Reconstructed context from authoritative artifacts and repository state/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Executed startup/bootstrap and regression pre-checks before new implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=h,e,l,l,o,Enter,sleep260,sleep260,Up,Down,q && cargo run -- --mode edit --tui --mcp-config .\docs\mcp-servers.example.json --mcp-refresh-ms 200 > target\regression-preflight-tui-iter29.log 2>&1"`
- Added and completed one new high-priority unfinished item:
  - Added `F-027`: reset MCP dedupe suppression counter when MCP status label changes, so `dN` represents suppressed duplicates within the active status phase.
  - Updated `src/tui/mod.rs` (`App::apply_mcp_refresh`) to reset `mcp_refresh_dedup_count` on status transition before applying same-signature dedupe logic.
  - Added test `tui::tests::status_transition_resets_refresh_dedup_counter`.
- Updated docs/tracking artifacts:
  - `README.md` clarifies that status-bar `dN` is scoped to the current MCP status label.
  - `feature_list.json` extended with `F-027` and verification evidence.

Verified checklist items
- `F-027` passed with:
  - `cargo fmt`
  - `cargo test status_transition_resets_refresh_dedup_counter` (1 passed, 0 failed)
  - `cargo test` (51 lib tests + 10 bin tests, 0 failed)
  - Real UI workflow evidence:
    - `cmd /c "set FASTCODE_TUI_SCRIPT=resize100x30,sleep260,sleep260,sleep260,sleep260,sleep260,h,i,Enter,Up,Down,q && cargo run -- --mode edit --tui --mcp-config .\target\mcp-flaky-config.json --mcp-refresh-ms 200 > target\f027-dedup-reset-transition.log 2>&1"`
    - Parsed log `target/f027-dedup-reset-transition.clean.log` includes pre-transition `mcp: error r1 d0`, transition snapshot `invalid-config r4 d0` (reset), then later `mcp: invalid-configr6 d2`; user interaction remained functional (`user: hi`, `assistant: rece...`).

Issues found and fixed
- Observability semantics gap: prior implementation accumulated `dN` across status phases, making it hard to tell suppression volume for the currently active MCP health state.
- Fix: reset `dN` when `mcp_status_label` changes while preserving global refresh counter (`rN`) and existing duplicate suppression behavior.

Next Priority
1. Add a focused UI test for status transition from repeated error to healthy (`ok`) in a deterministic scripted harness without runtime config mutation.
2. Improve log-cleaning tooling for ANSI-heavy scripted TUI evidence to simplify extracting status snapshots.
3. Consider surfacing last status-transition timestamp in status/help text for operational debugging.

Completion Snapshot
- 31/31 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 30)

Completed this session
- Reconstructed context from authoritative artifacts and repository state/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Executed startup/bootstrap and regression pre-checks before new implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=h,e,l,l,o,Enter,sleep260,sleep260,Up,Down,q && cargo run -- --mode edit --tui --mcp-config .\\docs\\mcp-servers.example.json --mcp-refresh-ms 200 > target\\regression-preflight-iter30.log 2>&1"`
- Added and completed one new high-priority unfinished item:
  - Added `F-028`: deterministic scripted MCP refresh harness coverage for `error -> ok` transition semantics.
  - Refactored TUI run loop in `src/tui/mod.rs` so MCP refresh source is injected as a callback in `run_loop`, while production behavior remains unchanged through `--mcp-config` mapping to `refresh_mcp_diagnostics`.
  - Added test `tui::tests::scripted_loop_error_to_ok_transition_resets_dedup_and_keeps_user_flow` validating sequence `error/error/ok/ok`, dedupe reset behavior (`dN`) on status transition, and preserved user interaction flow.
- Updated docs/tracking artifacts:
  - `feature_list.json` extended with `F-028` and verification evidence.

Verified checklist items
- `F-028` passed with:
  - `cargo fmt`
  - `cargo test scripted_loop_error_to_ok_transition_resets_dedup_and_keeps_user_flow` (1 passed, 0 failed)
  - `cargo test` (52 lib tests + 10 bin tests, 0 failed)
  - Real UI workflow evidence in `target/regression-preflight-iter30.log` (`target/regression-preflight-iter30.clean.log` includes MCP status + user interaction markers).

Issues found and fixed
- Testability gap: MCP refresh transition semantics in the event loop were only indirectly covered and coupled to runtime config lifecycle checks.
- Fix: injected diagnostics provider into TUI run loop for deterministic scripted verification without runtime config mutation.

Next Priority
1. Add a focused deterministic loop test for `ok -> error -> ok` oscillation and verify dedupe/message append semantics remain stable over multiple phase flips.
2. Improve ANSI log normalization utility to produce cleaner machine-parsable evidence from scripted TUI sessions.
3. Consider exposing the latest MCP status-transition timestamp in TUI status/help text for operations troubleshooting.

Completion Snapshot
- 32/32 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 31)

Completed this session
- Reconstructed context from authoritative artifacts and repository state/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Confirmed baseline tracker state and counts from `feature_list.json`:
  - `total=32`, `passed=32`, `unpassed=0`, `blocked=0`
- Executed startup/bootstrap and regression pre-checks before new implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=h,e,l,l,o,Enter,sleep260,sleep260,Up,Down,q && cargo run -- --mode edit --tui --mcp-config .\docs\mcp-servers.example.json --mcp-refresh-ms 200 > target\regression-preflight-iter31.log 2>&1"`
- Added and completed one new high-priority unfinished item:
  - Added `F-029`: deterministic coverage for MCP refresh dedupe stability during `ok -> error -> ok` oscillation.
  - Updated `src/tui/mod.rs` with test `tui::tests::scripted_loop_ok_error_ok_oscillation_resets_dedup_per_phase` to validate:
    - sequence `ok/ok/error/error/ok/ok`
    - dedupe reset semantics per status phase
    - monotonic refresh count with phase-first summary append behavior
    - preserved user input/submit flow (`user: hi`, `assistant: received -> hi`)
- Updated docs/tracking artifacts:
  - `feature_list.json` extended with `F-029` and verification evidence.

Verified checklist items
- `F-029` passed with:
  - `cargo test scripted_loop_ok_error_ok_oscillation_resets_dedup_per_phase` (1 passed, 0 failed)
  - `cargo test` (53 lib tests + 10 bin tests, 0 failed)
  - Real UI workflow evidence:
    - `$env:FASTCODE_TUI_SCRIPT='resize100x30,h,i,Enter,sleep260,sleep260,Up,Down,q'; cargo run -- --mode edit --tui --mcp-config ./docs/mcp-servers.example.json --mcp-refresh-ms 200 *> target/f029-oscillation-regression.log`
    - Evidence file retained: `target/f029-oscillation-regression.clean.log`.

Issues found and fixed
- No functional regression found in pre-checks.
- Evidence-capture issue: PowerShell redirection with TUI introduced heavy ANSI/control sequences and noisy `NativeCommandError` formatting.
- Mitigation: retained raw session log and generated cleaned log artifact for audit traceability.

Next Priority
1. Add a reliable ANSI normalization helper script in `scripts/` to produce machine-parsable `.clean.log` artifacts deterministically.
2. Add deterministic oscillation coverage for summary-content changes across unchanged status labels (for example `ok` with varying summaries) to confirm append/dedup boundaries remain correct.
3. Consider exposing last MCP status-transition timestamp in status/help text for operations troubleshooting.

Completion Snapshot
- 33/33 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 32)

Completed this session
- Reconstructed context from authoritative artifacts and repository state/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Confirmed baseline tracker state before implementation:
  - `total=33`, `passed=33`, `unpassed=0`, `blocked=0`
- Executed startup/bootstrap and regression pre-checks before new implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=h,e,l,l,o,Enter,sleep120,Up,Down,q && cargo run -- --mode edit --tui > target\regression-ui-core-iter32.log 2>&1"`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=resize80x24,h,i,Enter,sleep260,sleep260,v,Up,Down,q && cargo run -- --mode edit --tui --mcp-config .\docs\mcp-servers.example.json --mcp-refresh-ms 200 > target\regression-ui-mcp-iter32.log 2>&1"`
- Added and completed one new high-priority unfinished item:
  - Added `F-030`: normalize ANSI-heavy scripted TUI logs into machine-parsable evidence.
  - Implemented `scripts/normalize-tui-log.ps1` with `events`/`strip` modes and deterministic output path handling.
  - Updated `README.md` with normalization usage and mode definitions.

Verified checklist items
- `F-030` passed with:
  - `powershell -ExecutionPolicy Bypass -File ./scripts/normalize-tui-log.ps1 -InputPath ./target/regression-ui-core-iter32.log -OutputPath ./target/regression-ui-core-iter32.clean.log -Mode events`
  - `powershell -ExecutionPolicy Bypass -File ./scripts/normalize-tui-log.ps1 -InputPath ./target/regression-ui-mcp-iter32.log -OutputPath ./target/regression-ui-mcp-iter32.clean.log -Mode events`
  - `powershell -ExecutionPolicy Bypass -File ./scripts/normalize-tui-log.ps1 -InputPath ./target/regression-ui-mcp-iter32.log -OutputPath ./target/regression-ui-mcp-iter32.strip.log -Mode strip`
  - Validation assertions passed: no ESC bytes remained in clean outputs; required markers present (`fastcode | mode: edit | status: idle`, `mcp: error r1 d0`, MCP diagnostics lines).

Issues found and fixed
- First implementation left residual cursor-control fragments in normalized logs.
- Fixed by hardening ANSI/control removal and narrowing extraction to stable status/message patterns.

Next Priority
1. Add deterministic tests for the normalization script against canned ANSI fixture logs (pass/fail assertions for key marker extraction).
2. Optionally add `-MaxEventLength` and `-NoDedupe` switches for evidence tuning in noisy sessions.
3. Evaluate small Rust helper for ANSI stripping to share logic between runtime and tooling.

Completion Snapshot
- 34/34 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 33)

Completed this session
- Reconstructed context from authoritative artifacts and repository state/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Confirmed tracker baseline before implementation:
  - `total=34`, `passed=34`, `unpassed=0`, `blocked=0`
- Executed startup/bootstrap and regression pre-checks before new implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=h,e,l,l,o,Enter,sleep120,Up,Down,q && cargo run -- --mode edit --tui > target\regression-preflight-core-iter33.log 2>&1"`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=resize80x24,h,i,Enter,sleep260,sleep260,v,Up,Down,q && cargo run -- --mode edit --tui --mcp-config .\docs\mcp-servers.example.json --mcp-refresh-ms 200 > target\regression-preflight-mcp-iter33.log 2>&1"`
- Added and completed one new high-priority unfinished item:
  - Added `F-031`: operator-tunable TUI log normalization controls (`-MaxEventLength`, `-NoDedupe`) with deterministic script-level checks.
  - Updated `scripts/normalize-tui-log.ps1`:
    - Added `-MaxEventLength` (`40..4000`, default `240`).
    - Added `-NoDedupe` switch.
    - Refactored events extraction to line-based matching to keep dedupe/truncation behavior deterministic.
  - Added `scripts/test-normalize-tui-log.ps1` to validate default dedupe, no-dedupe, and length-capping behavior.
- Updated docs/tracking artifacts:
  - `README.md` normalization section now documents `-MaxEventLength`, `-NoDedupe`, and script-level verification command.
  - `feature_list.json` extended with `F-031` and verification evidence.

Verified checklist items
- `F-031` passed with:
  - `powershell -ExecutionPolicy Bypass -File ./scripts/test-normalize-tui-log.ps1` (`normalize-tui-log script tests: PASS`)
  - `powershell -ExecutionPolicy Bypass -File ./scripts/normalize-tui-log.ps1 -InputPath ./target/regression-preflight-mcp-iter33.log -OutputPath ./target/f031-events-short.log -Mode events -MaxEventLength 90 -NoDedupe`
  - `cargo test` (53 lib tests + 10 bin tests, 0 failed)

Issues found and fixed
- Script test strict-mode bug: `.Count` access on non-array pipeline outputs could fail when output collapsed to a scalar.
- Fix: wrapped repeated-message query pipelines with array coercion (`@(...)`) in `scripts/test-normalize-tui-log.ps1`.
- Extraction robustness gap: prior `Extract-TuiEvents` whitespace normalization could collapse event boundaries, weakening dedupe semantics.
- Fix: switched to line-oriented extraction with explicit status/message pattern checks.

Next Priority
1. Add fixture-based regression tests that assert `events` extraction stability against real captured noisy MCP logs.
2. Improve `events` extraction token coverage for richer punctuation while keeping false-positive noise low.
3. Optionally expose normalization summary stats (event count, dedupe drops) in script output for easier auditing.

Completion Snapshot
- 35/35 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 34)

Completed this session
- Reconstructed context from authoritative artifacts and repository state/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Confirmed tracker baseline before implementation:
  - `total=35`, `passed=35`, `unpassed=0`, `blocked=0`
- Executed startup/bootstrap and regression pre-checks before new implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=h,e,l,l,o,Enter,sleep120,Up,Down,q && cargo run -- --mode edit --tui > target\regression-preflight-core-iter34.log 2>&1"`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=resize80x24,h,i,Enter,sleep260,sleep260,v,Up,Down,q && cargo run -- --mode edit --tui --mcp-config .\docs\mcp-servers.example.json --mcp-refresh-ms 200 > target\regression-preflight-mcp-iter34.log 2>&1"`
- Added and completed one new high-priority unfinished item:
  - Added `F-032`: fixture-based regression checks for ANSI-heavy noisy TUI log normalization.
  - Updated `scripts/normalize-tui-log.ps1` to use token-ordered event extraction with explicit status/message matching and stable single-line normalization.
  - Added fixture template `scripts/fixtures/normalize-tui-log/noisy-mcp-fixture.template.txt`.
  - Added regression script `scripts/test-normalize-tui-log-fixtures.ps1`.
  - Updated `README.md` with fixture-test verification command.
  - Updated `feature_list.json` with `F-032` evidence.

Verified checklist items
- `F-032` passed with:
  - `powershell -ExecutionPolicy Bypass -File ./scripts/test-normalize-tui-log-fixtures.ps1` (`normalize-tui-log fixture tests: PASS`)
  - `powershell -ExecutionPolicy Bypass -File ./scripts/test-normalize-tui-log.ps1` (`normalize-tui-log script tests: PASS`)
  - `powershell -ExecutionPolicy Bypass -File ./scripts/normalize-tui-log.ps1 -InputPath ./target/regression-preflight-mcp-iter34.log -OutputPath ./target/regression-preflight-mcp-iter34.clean.log -Mode events`
  - `cargo test` (53 lib tests + 10 bin tests, 0 failed)

Issues found and fixed
- Regression preflight showed no panic-level failures, but normalized event logs still showed noisy concatenation on ANSI-heavy captures.
- Fixed by replacing line-split extraction with ordered token matching and adding canned fixture assertions that lock expected status/system/user/assistant extraction behavior.

Next Priority
1. Reduce residual message noise in real captures by tightening message-token stop conditions for malformed fragments while preserving true user/system content.
2. Add optional script output summary counters (for example extracted events and dedup-suppressed count) to improve evidence auditability.
3. Add fixture variants that cover mixed-width terminals and repeated resize events.

Completion Snapshot
- 36/36 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 35)

Completed this session
- Reconstructed context from authoritative artifacts and repository state/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Confirmed tracker baseline before implementation:
  - `total=36`, `passed=36`, `unpassed=0`, `blocked=0`
- Executed startup/bootstrap and regression pre-checks before new implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=h,e,l,l,o,Enter,sleep120,Up,Down,q && cargo run -- --mode edit --tui > target\regression-preflight-core-iter35.log 2>&1"`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=resize80x24,h,i,Enter,sleep260,sleep260,v,Up,Down,q && cargo run -- --mode edit --tui --mcp-config .\docs\mcp-servers.example.json --mcp-refresh-ms 200 > target\regression-preflight-mcp-iter35.log 2>&1"`
- Added and completed one new high-priority unfinished item:
  - Added `F-033`: auditable normalization summary counters for TUI log evidence.
  - Updated `scripts/normalize-tui-log.ps1` with `-EmitSummary` and `-SummaryPath` and JSON summary fields for bytes/lines plus events counters (`event_candidate_count`, `event_output_line_count`, `dedupe_suppressed_count`, `truncated_count`).
  - Updated `scripts/test-normalize-tui-log.ps1` with summary assertions.
  - Updated `README.md` normalization options to document summary output switches.

Verified checklist items
- `F-033` passed with:
  - `powershell -ExecutionPolicy Bypass -File ./scripts/test-normalize-tui-log.ps1` (`normalize-tui-log script tests: PASS`)
  - `powershell -ExecutionPolicy Bypass -File ./scripts/test-normalize-tui-log-fixtures.ps1` (`normalize-tui-log fixture tests: PASS`)
  - `powershell -ExecutionPolicy Bypass -File ./scripts/normalize-tui-log.ps1 -InputPath ./target/regression-preflight-mcp-iter35.log -OutputPath ./target/f033-mcp-events.log -Mode events -SummaryPath ./target/f033-mcp-events-summary.json`
  - `powershell -ExecutionPolicy Bypass -File ./scripts/normalize-tui-log.ps1 -InputPath ./target/regression-preflight-core-iter35.log -OutputPath ./target/f033-core-events.log -Mode events -SummaryPath ./target/f033-core-events-summary.json`
  - Summary checks: `mcp_summary: mode=events out_lines=8 dedupe_suppressed=0 truncated=2`; `core_summary: mode=events out_lines=6 dedupe_suppressed=0 truncated=0`; both normalized outputs had `contains_esc=False`.
  - `cargo test` (53 lib tests + 10 bin tests, 0 failed)

Issues found and fixed
- No critical regressions found during preflight.
- Addressed evidence-audit gap: normalization pipeline previously lacked machine-readable counters for dedupe/truncation behavior; fixed via summary JSON output and test coverage.

Next Priority
1. Tighten token boundaries in `events` extraction so malformed ANSI-corrupted fragments are less likely to merge adjacent message labels in very noisy captures.
2. Add one fixture variant that intentionally interleaves resize bursts + MCP detail expansion to lock expected summary counter behavior over noisy transitions.
3. Optionally expose a compact `-SummaryFormat compact` mode for easier grep/CI ingestion.

Completion Snapshot
- 37/37 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 36)

Completed this session
- Reconstructed context from authoritative artifacts and repository state/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Confirmed tracker baseline before implementation:
  - `total=37`, `passed=37`, `unpassed=0`, `blocked=0`
- Executed startup/bootstrap and regression pre-checks before new implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=h,e,l,l,o,Enter,sleep120,Up,Down,q && cargo run -- --mode edit --tui > target\regression-preflight-core-iter36.log 2>&1"`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=resize80x24,h,i,Enter,sleep260,sleep260,v,Up,Down,q && cargo run -- --mode edit --tui --mcp-config .\docs\mcp-servers.example.json --mcp-refresh-ms 200 > target\regression-preflight-mcp-iter36.log 2>&1"`
  - Normalized preflight logs and confirmed no panic/error matches in raw logs.
- Added and completed one new high-priority unfinished item:
  - Added `F-034`: compact normalization summary output for CI/grep workflows.
  - Updated `scripts/normalize-tui-log.ps1` with `-SummaryFormat <json|compact>` and applied format selection to both `-EmitSummary` and `-SummaryPath`.
  - Updated `scripts/test-normalize-tui-log.ps1` to validate compact output key fields and retain existing JSON summary assertions.
  - Updated `README.md` normalization options to document `-SummaryFormat` behavior.
  - Updated `feature_list.json` with `F-034` evidence.

Verified checklist items
- `F-034` passed with:
  - `powershell -ExecutionPolicy Bypass -File ./scripts/test-normalize-tui-log.ps1` (`normalize-tui-log script tests: PASS`)
  - `powershell -ExecutionPolicy Bypass -File ./scripts/test-normalize-tui-log-fixtures.ps1` (`normalize-tui-log fixture tests: PASS`)
  - `powershell -ExecutionPolicy Bypass -File ./scripts/normalize-tui-log.ps1 -InputPath ./target/regression-preflight-mcp-iter36.log -OutputPath ./target/f034-mcp-events.log -Mode events -SummaryPath ./target/f034-mcp-events-summary.compact.txt -SummaryFormat compact`
  - Compact summary evidence in `target/f034-mcp-events-summary.compact.txt` includes key-value counters (`mode=events`, `event_output_line_count=8`, `truncated_count=2`).
  - `cargo test` (53 lib tests + 10 bin tests, 0 failed)

Issues found and fixed
- Initial `compact` summary implementation used string format syntax that failed in method-argument context.
- Fix: replaced format expression with explicit string interpolation (`$key=$value`) in summary formatter.
- Compact summary test originally assumed key order; order is not contractually guaranteed.
- Fix: adjusted assertions to require key presence instead of fixed prefix order.

Next Priority
1. Add a deterministic test asserting `-EmitSummary -SummaryFormat compact` stdout content in addition to summary-file coverage.
2. Consider adding optional stable key ordering for compact output if downstream tooling requires deterministic field order.
3. Add one fixture that includes multiple long MCP detail lines to validate compact summary truncation counters under heavier noise.

Completion Snapshot
- 38/38 checklist items passing.

---

Session Update - 2026-02-14 (Coding Agent, Iteration 37)

Completed this session
- Reconstructed context from authoritative artifacts and repository state/history in a fresh window:
  - `app_spec.txt`
  - `feature_list.json`
  - `README.md`
  - `claude-progress.txt`
  - `git log --oneline -20`
- Confirmed tracker baseline before implementation:
  - `total=38`, `passed=38`, `unpassed=0`, `blocked=0`
- Executed startup/bootstrap and regression pre-checks before new implementation:
  - `powershell -ExecutionPolicy Bypass -File ./init.ps1`
  - `cargo run -- --mode edit`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=h,e,l,l,o,Enter,sleep120,Up,Down,q && cargo run -- --mode edit --tui > target\regression-preflight-core-iter37.log 2>&1"`
  - `cmd /c "set FASTCODE_TUI_SCRIPT=resize80x24,h,i,Enter,sleep260,sleep260,v,Up,Down,q && cargo run -- --mode edit --tui --mcp-config .\docs\mcp-servers.example.json --mcp-refresh-ms 200 > target\regression-preflight-mcp-iter37.log 2>&1"`
  - Normalized preflight logs:
    - `powershell -ExecutionPolicy Bypass -File ./scripts/normalize-tui-log.ps1 -InputPath ./target/regression-preflight-core-iter37.log -OutputPath ./target/regression-preflight-core-iter37.clean.log -Mode events`
    - `powershell -ExecutionPolicy Bypass -File ./scripts/normalize-tui-log.ps1 -InputPath ./target/regression-preflight-mcp-iter37.log -OutputPath ./target/regression-preflight-mcp-iter37.clean.log -Mode events`
  - Checked logs for runtime regressions (`panic|thread 'main' panicked|ERROR|error:`) with no matches in both preflight logs.
- Added and completed one new high-priority unfinished item:
  - Added `F-035`: deterministic compact summary stdout verification for `-EmitSummary -SummaryFormat compact`.
  - Updated `scripts/test-normalize-tui-log.ps1` to invoke normalization with emit-summary compact mode, capture stdout, and assert summary label + stable key counters (`mode`, `event_candidate_count`, `event_output_line_count`, `dedupe_suppressed_count`, `truncated_count`).
  - Updated `feature_list.json` with `F-035` evidence.

Verified checklist items
- `F-035` passed with:
  - `powershell -ExecutionPolicy Bypass -File ./scripts/test-normalize-tui-log.ps1` (`normalize-tui-log script tests: PASS`)
  - `powershell -ExecutionPolicy Bypass -File ./scripts/test-normalize-tui-log-fixtures.ps1` (`normalize-tui-log fixture tests: PASS`)
  - `cargo test` (53 lib tests + 10 bin tests, 0 failed)

Issues found and fixed
- No critical regressions detected in preflight core/MCP UI workflows.
- No additional runtime or console errors introduced by this change.

Next Priority
1. Tighten `events` token stop conditions to further reduce merged noise fragments in ANSI-heavy real captures.
2. Add one fixture variant that interleaves repeated resize events with MCP detail expansion and verify summary counters remain stable.
3. Consider optional stable field ordering contract for compact summaries if external parsers require deterministic order.

Completion Snapshot
- 39/39 checklist items passing.
