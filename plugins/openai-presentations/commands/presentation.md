---
description: Create, outline, review, or improve a presentation deck.
argument-hint: "[presentation request]"
allow_edit: true
allow_commands: false
---

Use the Presentations skill for this request. Produce a slide-focused deliverable with a clear audience, goal, narrative arc, slide-level takeaway titles, content, visual direction, and optional speaker notes.

Request:

$ARGUMENTS

If the user asked for a file, prefer a Markdown deck spec unless another format is explicitly requested. Ask before using shell commands or external tools for PPTX generation.
